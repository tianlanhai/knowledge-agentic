# knowledge-agentic（后端）

本目录为后端服务代码（FastAPI + SQLAlchemy + LangChain/LangGraph）。

## 用途
- 本文件用于 Python 包元数据（`pyproject.toml` 的 `readme` 字段）与依赖安装/构建流程（例如 `uv sync`）。  
- 更完整的项目说明请参考仓库根目录与 `docs/` 下文档。

